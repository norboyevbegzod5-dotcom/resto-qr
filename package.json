{
  "name": "resto-qr",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "chmod +x build.sh && bash build.sh",
    "start": "cd backend && npm run start:prod",
    "migrate": "cd backend && npx prisma migrate deploy"
  }
}
